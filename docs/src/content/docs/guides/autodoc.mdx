---
title: Autodoc
description: Document a single Python object in the middle of a hand-written page.
---

import { Autodoc, SymbolSearch } from 'starlight-pydocs/components';

Generated pages cover a whole package, but a hand-written page sometimes needs one
class in the middle of the prose. That is what `<Autodoc>` is for: it is the equivalent
of mkdocstrings' `::: demopkg.report.Report` directive, and it renders exactly what the
generated pages do.

```mdx
import { Autodoc } from 'starlight-pydocs/components';

<Autodoc name="demopkg.report.Report" />
```

Names may be the path an object is documented at or the canonical path Griffe found the
definition at.

<Autodoc name="demopkg.report.Report" package="api/demopkg" />

## Choosing the package

The dotted name usually says which package an object belongs to, so `package` is optional.
It becomes required when one package is documented at more than one base, because then the
import name identifies two entries. Give it the base of the one you mean:

```mdx
<Autodoc name="demopkg.report.Report" package="api/demopkg" />
<Autodoc name="demopkg.report.Report" package="1x/api/demopkg" />
```

This site documents `demopkg` twice (the current source and the archived 1.x dump), which is
why the blocks on this page name their base. Without it the build stops with the two
candidate bases listed. `package` also accepts a plain import name where that is
unambiguous.

## Heading levels

`headingLevel` sets the depth of the object's own heading; its members go one level
deeper. Use it to slot a block under a section of your own without breaking the
document outline.

```mdx
<Autodoc name="demopkg.generate_report" headingLevel={3} />
```

<Autodoc name="demopkg.generate_report" package="api/demopkg" headingLevel={3} />

## Symbol search

`<SymbolSearch />` searches the API surface by name, client-side, from the
`symbols.json` index the plugin publishes. It is on the generated package pages
already; this is the same component placed by hand. It takes the same `package`
prop, a base or an unambiguous import name, and defaults to the first configured
entry.

<SymbolSearch package="api/demopkg" />
